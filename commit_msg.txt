Fix validate_config.py to only check public routes in health checks

- Modified build_routes_by_name() to accept include_private and include_builtin flags
- cmd_validate() validates ALL routes (including private) for completeness  
- cmd_healthcheck() and cmd_full_check() Phase 2 only test PUBLIC user routes:
  - Excludes private routes (is_private=True)
  - Excludes builtin routes (builtin/*)
- Added optional routes_to_check parameter to run_health_check() in healthcheck.py

Result: 
- Validation: 23 routes checked (all routes including private/builtin)
- Health check: 17 routes checked (public user routes only)
