"""Orchestration-internal event helpers.

Community-facing filter modules live in :mod:`keeprollming.filters`.  This
package remains only because request filters emit orchestration events from
``events.py``.
"""
